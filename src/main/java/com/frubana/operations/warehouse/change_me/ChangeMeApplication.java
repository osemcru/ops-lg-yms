package com.frubana.operations.warehouse.change_me;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;


/** Application starter.
 */
@SpringBootApplication
public class ChangeMeApplication {

  /** Main method that runs the spring boot.
   *
   * @param args args to run the spring application.
   */
  public static void main( String[] args ) {
    SpringApplication.run(ChangeMeApplication.class, args);
  }
}
